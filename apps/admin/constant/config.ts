import type { SiteConfig } from '@cubid/types';

export const siteConfig = {
  title: 'Cubid Admin',
  description:
    'Administrative control plane for Cubid app configuration, pages, API keys, and webhooks.',
  url: 'https://tsnext-tw.thcl.dev',
} satisfies SiteConfig;
