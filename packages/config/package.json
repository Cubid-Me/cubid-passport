{
  "name": "@cubid/config",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "exports": {
    ".": "./src/index.ts"
  },
  "files": [
    "src",
    "dist"
  ],
  "scripts": {
    "build": "pnpm exec tsc -p tsconfig.json",
    "lint": "pnpm exec tsc --noEmit -p tsconfig.json",
    "typecheck": "pnpm exec tsc --noEmit -p tsconfig.json",
    "test": "echo \"No tests defined for @cubid/config\"",
    "format": "echo \"No format changes required for @cubid/config\"",
    "format:write": "echo \"No format changes required for @cubid/config\""
  },
  "devDependencies": {
    "@types/node": "^20.17.10",
    "typescript": "5.9.3"
  }
}
