{
  "name": "@cubid/identity",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "exports": {
    ".": "./src/index.ts"
  },
  "files": [
    "src",
    "dist"
  ],
  "scripts": {
    "build": "pnpm exec tsc -p tsconfig.json",
    "lint": "pnpm exec tsc --noEmit -p tsconfig.json",
    "typecheck": "pnpm exec tsc --noEmit -p tsconfig.json",
    "test": "pnpm exec tsx --test src/**/*.test.ts",
    "format": "echo \"No format changes required for @cubid/identity\"",
    "format:write": "echo \"No format changes required for @cubid/identity\""
  },
  "devDependencies": {
    "@types/node": "^20.17.10",
    "tsx": "^4.19.4",
    "typescript": "5.2.2"
  }
}